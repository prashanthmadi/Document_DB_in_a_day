# Azure DocumentDB Training - Python Dependencies
# Install with: pip install -r requirements.txt

# Azure OpenAI SDK
# Used in: Module 3 (embeddings), Module 4 (agent embedding tool)
# Purpose: Generate embeddings for vector search
openai>=1.12.0

# Environment variable management
# Used in: Module 3, Module 4
# Purpose: Load API keys and configuration from .env files
python-dotenv>=1.0.0

# MongoDB Python Driver
# Used in: Module 3 (healthcare), Module 4 (agent tools)
# Purpose: Connect to Azure DocumentDB and perform operations
pymongo>=4.6.0

# Microsoft Agent Framework (preview)
# Used in: Module 4 (AI Agents)
# Purpose: Build AI agents with function tools
agent-framework[azure]

# Microsoft Agent Framework DevUI (preview)
# Used in: Module 4 (AI Agents)
# Purpose: Interactive web UI for testing agents
agent-framework-devui

# Pydantic (required by Agent Framework for tool parameter descriptions)
pydantic>=2.0.0
